#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints name of program, followed by new name
 * @argc: argument count
 * @argv: argument string
 * Return: always 0
 */
int main(int argc, char *argv[])

{
	printf("%s\n,", argv[0]);
	return (0);
}
